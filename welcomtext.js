const welcome = (number, groupname) => {
    return `𝐎𝐥𝐚 @${number}. 𝐒�ea 𝐛�ienvenido �al 𝐠𝐫𝐮𝐩𝐨 ${groupname}`
}
exports.welcome = welcome

const bye = (number) => {
    return `Adeus @${number}. CHAU GORDO DOWN 👋`
}
exports.bye = bye
